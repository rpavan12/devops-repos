---
- hosts: web
  become: yes
  tasks:
    - name: install nginx
      yum:
        name: nginx
        state: present # uninstall nginx
    - name: display nginx details
      debug:
        msg: 
          - "we have installed {{ myname }} and version is {{ version }}"
          - " now the user is {{ user }}"